Create a polished privacy-focused finance data workflow infographic for a GitHub README, exactly 1536x1024, 3:2 landscape, white to very light gray background, dark navy text, restrained green, blue, gold, and teal accents, clean rounded cards, large readable Japanese labels, precise arrows, no logos, no watermark, no decorative clutter. Title: 「Local Finance Dashboard のしくみ」. On the left, show two input groups. Expense inputs: 「Gmail 利用通知」 and 「支出CSV」. Asset inputs: 「証券資産CSV」, 「取引履歴CSV」, 「公開基準価額」. Both flow into a central card: 「形式ごとに検証・正規化」 with note 「source_id + external_id で重複防止」. Continue to 「ローカル SQLite」 → 「ローカル HTTP API 127.0.0.1:8765」 → 「Web UI」. Inside the Web UI output, show two tabs: 「家計簿」 and 「資産」, plus 「期間・口座・カテゴリ別集計」. Add a separate lower output from SQLite: 「読み取り専用HTML / オフライン閲覧」. Add a shield note: 「Gmail本文全文・実CSV・DBは外部公開しない」. Add a small optional dashed card: 「Codex App Server 分析（任意）」 connected to the API, but make clear the dashboard continues without it. Arrows must not cross and labels must remain readable at README width.
